# Backend Instructions

No traditional backend required.

Create optional API route:

/api/resolve

Purpose:
Fallback ENS resolution using viem public client.

Functions:
- resolve ENS name from address
- fetch ENS text record

Return:
{
  ens,
  preferences
}

Keep code minimal.
